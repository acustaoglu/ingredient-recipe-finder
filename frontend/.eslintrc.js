// .eslintrc.js

module.exports = {
    env: {
      browser: true,
      es2021: true,
    },
    extends: [
      'react-app', // Extends the Create React App ESLint configuration
      // Remove 'plugin:react-hooks/recommended' if present
      // Remove 'eslint:recommended' if already included in react-app
      // Add other necessary extends here if needed
    ],
    plugins: [
      // Remove 'react-hooks' if present
      // Add other plugins if necessary
    ],
    rules: {
      // Remove any 'react-hooks/*' rules if present
      // Add or customize other rules as needed
    },
  };
  